/**
 * @deepseek-ai/dsh-tui-app — the interactive full-screen terminal surface as
 * a profile bundle. The package's substance is `cordis.patch.yml` (declared
 * by the `dsh.bundle.patch` manifest field and resolved by the profile
 * composer through that field) plus the `./startup` provider row it inserts;
 * this module carries no runtime API.
 * @module @deepseek-ai/dsh-tui-app
 */

export {}
